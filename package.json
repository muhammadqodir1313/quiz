{
  "name": "quiz-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "homepage": "https://muhammadqodir1313.github.io/frontend_quiz",
  "description": "Quiz platform with admin panel and user interface",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.0",
    "axios": "^1.6.7",
    "tailwindcss": "^3.4.1",
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.1.0",
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "start": "concurrently \"cd server && npm start\" \"cd client && npm start\"",
    "build": "cd client && npm run build",
    "deploy": "cd client && npm run deploy"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "gh-pages": "^6.1.1",
    "concurrently": "^8.2.2"
  }
}
